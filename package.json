{
  "name": "membridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun --env-file=.env.test test"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "postgres": "^3.4.5"
  }
}
